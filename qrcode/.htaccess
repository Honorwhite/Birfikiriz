RewriteEngine On

# Allow real files and assets
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA fallback for React Router
RewriteRule . /qrcode/index.html [L]